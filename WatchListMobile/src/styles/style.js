import {scale, verticalScale} from 'react-native-size-matters';

export const colors = {
    black: '#000000',
    blue: '#0061ff',
};

export const width = {
    full: '100%',
    half: '50%',
    quarter: '25%',
}

export const height = {
    full: '100%',
    half: '50%',
    quarter: '25%',
}

export const padding = {
    xs: scale(5),
    sm: scale(10),
    md: scale(20),
    lg: scale(30),
    xl: scale(40),
};

export const textSize = {
    sm: scale(10),
    md: scale(15),
    lg: scale(20),
};